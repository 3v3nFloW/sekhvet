## What this changes

## How it was checked

Changes to `DCMView`, `MPR*` or `ROI` need a measurement check on a real image (which modality, which device, what value before and after).

## Privacy

- [ ] No owner names, animal names, patient IDs or real DICOM files in the diff or the description.
